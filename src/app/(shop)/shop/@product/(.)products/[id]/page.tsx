import React from 'react';

export default function ProductModalPage({ params: { id } }: { params: { id: string } }) {
  return (
    <div className="modal">
      MyModal TESTgfdgfddgfd
    </div>
  )
}